<!DOCTYPE html>
<html lang="vi">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Login</title>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: 'Roboto', sans-serif;
      }

      body {
        min-height: 100vh;
        background: linear-gradient(135deg, #1a1a1a 0%, #2d3436 100%);
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
      }

      .login-container {
        width: 100%;
        max-width: 1000px;
        background: rgba(255, 255, 255, 0.95);
        border-radius: 20px;
        box-shadow: 0 15px 35px rgba(0, 0, 0, 0.2);
        overflow: hidden;
        display: flex;
        min-height: 600px;
        position: relative;
      }

      .back-button {
        position: absolute;
        top: 20px;
        left: 20px;
        background: rgba(255, 255, 255, 0.9);
        border: none;
        border-radius: 50%;
        width: 40px;
        height: 40px;
        display: flex;
        align-items: center;
        justify-content: center;
        cursor: pointer;
        transition: all 0.3s ease;
        z-index: 2;
        text-decoration: none;
      }

      .back-button:hover {
        transform: scale(1.1);
        background: white;
      }

      .back-button i {
        color: #2d3748;
        font-size: 1.2rem;
      }

      .login-image {
        flex: 1;
        background: url('https://wallpaperset.com/w/full/d/a/3/213529.jpg') center/cover;
        position: relative;
        display: flex;
        align-items: center;
        justify-content: center;
      }

      .login-image::before {
        content: '';
        position: absolute;
        top: 0;
        left: 0;
        right: 0;
        bottom: 0;
        background: rgba(0, 0, 0, 0.6);
      }

      .nike-logo {
        width: 200px;
        height: auto;
        position: relative;
        z-index: 1;
      }

      .login-form {
        flex: 1;
        padding: 50px;
        display: flex;
        flex-direction: column;
        justify-content: center;
      }

      .login-header {
        text-align: center;
        margin-bottom: 40px;
      }

      .login-header .logo {
        width: 80px;
        height: auto;
        margin-bottom: 20px;
      }

      .login-header h1 {
        color: #2d3748;
        font-size: 2rem;
        margin-bottom: 10px;
      }

      .login-header p {
        color: #718096;
        font-size: 0.95rem;
      }

      .form-group {
        margin-bottom: 25px;
        position: relative;
      }

      .form-group i {
        position: absolute;
        left: 15px;
        top: 50%;
        transform: translateY(-50%);
        color: #a0aec0;
      }

      .form-control {
        width: 100%;
        padding: 15px 15px 15px 45px;
        border: 2px solid #e2e8f0;
        border-radius: 10px;
        font-size: 1rem;
        transition: all 0.3s ease;
        background: #f7fafc;
      }

      .form-control:focus {
        border-color: #2d3748;
        box-shadow: 0 0 0 3px rgba(45, 55, 72, 0.1);
        outline: none;
      }

      .form-control::placeholder {
        color: #a0aec0;
      }

      .btn-login {
        width: 100%;
        padding: 15px;
        background: #2d3748;
        border: none;
        border-radius: 10px;
        color: white;
        font-size: 1rem;
        font-weight: 500;
        cursor: pointer;
        transition: all 0.3s ease;
      }

      .btn-login:hover {
        transform: translateY(-2px);
        box-shadow: 0 5px 15px rgba(45, 55, 72, 0.4);
        background: #1a202c;
      }

      .login-footer {
        text-align: center;
        margin-top: 30px;
      }

      .terms {
        display: flex;
        justify-content: center;
        gap: 20px;
        margin-top: 20px;
      }

      .terms a {
        color: #718096;
        text-decoration: none;
        font-size: 0.85rem;
        transition: color 0.3s ease;
      }

      .terms a:hover {
        color: #2d3748;
      }

      .error-message {
        background: #fff5f5;
        color: #c53030;
        padding: 10px;
        border-radius: 8px;
        margin-bottom: 20px;
        font-size: 0.9rem;
        border: 1px solid #feb2b2;
        display: flex;
        align-items: center;
        gap: 8px;
      }

      @media (max-width: 768px) {
        .login-container {
          flex-direction: column;
        }

        .login-image {
          display: none;
        }

        .login-form {
          padding: 30px;
        }

        .back-button {
          top: 10px;
          left: 10px;
        }
      }
    </style>
  </head>
  <body>
    <div class="login-container">
      <a href="/" class="back-button">
        <i class="fas fa-arrow-left"></i>
      </a>
      <div class="login-image">
        <img src="https://i.pinimg.com/originals/33/e6/3d/33e63d5adb0da6b303a83901c8e8463a.png" alt="Nike Logo" class="nike-logo">
      </div>
      <div class="login-form">
        <div class="login-header">
          <img src="https://i.pinimg.com/originals/33/e6/3d/33e63d5adb0da6b303a83901c8e8463a.png" alt="Logo" class="logo">
          <h1>Admin Dashboard</h1>
          <p>Đăng nhập để quản lý hệ thống</p>
        </div>

        <% if (error) { %>
          <div class="error-message">
            <i class="fas fa-exclamation-circle"></i> <%= error %>
          </div>
        <% } %>

        <form action="<%-prefixAdmin%>/auth/login" method="post">
          <div class="form-group">
            <i class="fas fa-envelope"></i>
            <input
              type="email"
              class="form-control"
              name="email"
              placeholder="Email của bạn"
              required
            />
          </div>

          <div class="form-group">
            <i class="fas fa-lock"></i>
            <input
              type="password"
              class="form-control"
              name="password"
              placeholder="Mật khẩu"
              required
            />
          </div>

          <button type="submit" class="btn-login">
            <i class="fas fa-sign-in-alt"></i> Đăng nhập
          </button>
        </form>

        <div class="login-footer">
          <div class="terms">
            <a href="#!">Điều khoản sử dụng</a>
            <a href="#!">Chính sách bảo mật</a>
          </div>
        </div>
      </div>
    </div>
  </body>
</html>
