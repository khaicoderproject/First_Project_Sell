<nav class="navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top">
  <div class="container">
  <!-- Logo -->
  <a class="navbar-brand" href="/">
    <img
      src="https://i.pinimg.com/originals/33/e6/3d/33e63d5adb0da6b303a83901c8e8463a.png"
      alt="Logo"
        class="logo-img"
    />
  </a>

    <!-- Navbar Toggler -->
  <button
      class="navbar-toggler border-0"
    type="button"
    data-toggle="collapse"
    data-target="#navbarSupportedContent"
    aria-controls="navbarSupportedContent"
    aria-expanded="false"
    aria-label="Toggle navigation"
  >
    <span class="navbar-toggler-icon"></span>
  </button>

  <!-- Navbar Menu -->
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <!-- Main Navigation -->
      <ul class="navbar-nav mx-auto dm-main-nav">
        <li class="nav-item">
          <a class="nav-link font-weight-bold dm-nav-link" href="/">
            <i class="fas fa-home dm-nav-icon"></i><span>TRANG CHỦ</span>
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link dm-nav-link" href="/product">
            <i class="fas fa-box dm-nav-icon"></i><span>SẢN PHẨM</span>
          </a>
        </li>
        <li class="nav-item dropdown">
          <a class="nav-link dm-nav-link dropdown-toggle" 
             href="#" 
             id="categoryDropdown" 
             role="button" 
             data-toggle="dropdown" 
             aria-haspopup="true" 
             aria-expanded="false">
            <i class="fas fa-tag dm-nav-icon"></i><span>DANH MỤC</span>
          </a>
          <div class="dropdown-menu dm-category-menu" aria-labelledby="categoryDropdown">
            <% category.forEach(item => { %>
              <a class="dropdown-item dm-dropdown-item" href="/category/<%=item.slug%>">
                <i class="fas fa-angle-right dm-dropdown-icon mr-2"></i><%=item.title.toUpperCase()%>
              </a>
            <% }) %>
          </div>
        </li>
        <li class="nav-item">
          <a class="nav-link dm-nav-link" href="/news">
            <i class="fas fa-newspaper dm-nav-icon"></i><span>TIN TỨC</span>
          </a>
        </li>
        <%if(user){%>
          <li class="nav-item">
            <a class="nav-link dm-nav-link" href="/post">
              <i class="fas fa-pen-fancy dm-nav-icon"></i><span>BÀI VIẾT</span>
            </a>
          </li>
        <%}%>
      </ul>

      <!-- Right Section -->
      <div class="dm-navbar-right d-flex align-items-center">
    <!-- Search Form -->
        <form class="dm-search-form mx-3" action="/product" method="GET">
          <div class="input-group">
      <input
              class="form-control dm-search-input"
        type="text"
        name="search"
        placeholder="Tìm kiếm sản phẩm..."
        aria-label="Search"
      />
            <div class="input-group-append">
              <button class="btn btn-link dm-search-btn" type="submit">
                <i class="fas fa-search"></i>
      </button>
            </div>
          </div>
    </form>

        <!-- User Actions -->
        <div class="dm-user-actions d-flex align-items-center">
          <!-- Cart -->
          <a class="nav-link mx-2 position-relative dm-nav-link" href="/cart" data-toggle="tooltip" title="Giỏ hàng">
            <i class="fas fa-shopping-cart dm-nav-icon"></i>
            <span class="badge badge-pill badge-primary position-absolute dm-badge">
              <%= (typeof cartQuantity !== 'undefined' && cartQuantity.quantityTotal) || 0 %>
      </span>
    </a>

          <!-- Notifications -->
    <div class="dropdown mx-2">
      <a
              class="nav-link position-relative dm-nav-link"
        id="notificationDropdown"
        role="button"
        data-toggle="dropdown"
        aria-haspopup="true"
        aria-expanded="false"
              title="Thông báo"
      >
              <i class="fas fa-bell dm-nav-icon"></i>
              <span class="badge badge-pill badge-primary position-absolute dm-badge">5</span>
      </a>
            <div class="dropdown-menu dropdown-menu-right dm-notification-menu" aria-labelledby="notificationDropdown">
              <div class="dm-notification-header">
                <h6 class="mb-0">Thông báo</h6>
                <a href="#" class="text-primary">Đánh dấu đã đọc</a>
              </div>
              <div class="dm-notification-body">
                <div class="dm-notification-item">
                  <img src="https://via.placeholder.com/40" alt="User" class="rounded-circle" />
                  <div class="dm-notification-content">
                    <p class="mb-1"><b>Tara Wesler</b> đã chấp nhận yêu cầu họp của bạn.</p>
                    <small class="text-muted">4 phút trước</small>
                  </div>
                </div>
              </div>
              <div class="dm-notification-footer">
                <a href="/notifications" class="btn btn-link btn-block">Xem tất cả</a>
              </div>
        </div>
          </div>

          <!-- User Profile -->
          <% if (user) { %>
          <div class="dropdown">
            <a class="nav-link dropdown-toggle d-flex align-items-center dm-nav-link"
               href="#" 
               id="userDropdown" 
               role="button" 
               data-toggle="dropdown" 
               aria-haspopup="true" 
               aria-expanded="false">
              <i class="fas fa-user-circle dm-nav-icon mr-2"></i>
              <span><%=user.fullName%></span>
            </a>
            <div class="dropdown-menu dropdown-menu-right dm-dropdown-menu" aria-labelledby="userDropdown">
              <a class="dropdown-item dm-dropdown-item" href="/user/profile/<%=user.userIdentifier%>">
                <i class="fas fa-user dm-dropdown-icon mr-2"></i>Hồ sơ
              </a>
              <a class="dropdown-item dm-dropdown-item" href="/user/setting-profile/<%=user.userIdentifier%>">
                <i class="fas fa-cog dm-dropdown-icon mr-2"></i>Cài đặt
              </a>
              <div class="dropdown-divider"></div>
              <a class="dropdown-item text-danger dm-dropdown-item" href="/auth/logout">
                <i class="fas fa-sign-out-alt dm-dropdown-icon mr-2"></i>Đăng xuất
              </a>
            </div>
          </div>
          <% } else { %>
          <a class="nav-link mx-2 dm-nav-link" href="/auth/login" data-toggle="tooltip" title="Đăng nhập">
            <i class="fas fa-user dm-nav-icon"></i>
          </a>
          <% } %>
        </div>
      </div>
    </div>
  </div>
</nav>

    <style>
  /* Font Import */
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap');

  /* Base Styles */
  .navbar {
    padding: 0.5rem 0;
    font-family: 'Inter', sans-serif;
    background: rgba(255, 255, 255, 0.98) !important;
    backdrop-filter: blur(10px);
  }

  /* Logo */
  .logo-img {
    width: 40px;
    height: auto;
    transition: transform 0.3s ease;
  }
  .navbar-brand:hover .logo-img {
    transform: scale(1.05);
  }

  /* Navigation */
  .dm-main-nav {
    margin: 0 1rem;
  }
  .dm-nav-link {
    color: #111 !important;
    font-size: 0.85rem;
    font-weight: 500;
    letter-spacing: 0.5px;
    padding: 0.5rem 0.8rem !important;
    transition: all 0.3s ease;
    display: flex;
    align-items: center;
    white-space: nowrap;
  }
  .dm-nav-icon {
    font-size: 0.9rem;
    margin-right: 0.4rem;
    opacity: 0.8;
    transition: all 0.3s ease;
  }
  .dm-nav-link span {
    white-space: nowrap;
  }
  .dm-nav-link:hover {
    color: #007bff !important;
    transform: translateY(-1px);
  }
  .dm-nav-link:hover .dm-nav-icon {
    opacity: 1;
    transform: scale(1.1);
  }

  /* Category Menu */
  .dm-category-menu {
        max-height: 400px;
        overflow-y: auto;
    border: none;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    border-radius: 10px;
    min-width: 200px;
    padding: 0.5rem 0;
  }
  .dm-category-menu .dropdown-item {
    padding: 0.6rem 1rem;
    font-size: 0.9rem;
    color: #333;
    letter-spacing: 0.3px;
    display: flex;
    align-items: center;
    transition: all 0.3s ease;
  }
  .dm-category-menu .dropdown-item:hover {
    background: #f8f9fa;
    color: #007bff;
    transform: translateX(5px);
  }

  /* Search Form */
  .dm-search-form {
    position: relative;
  }
  .dm-search-form .input-group {
    width: 240px;
    background: #f5f5f5;
    border-radius: 30px;
    overflow: hidden;
    transition: all 0.3s ease;
  }
  .dm-search-input {
    border: none;
    background: transparent;
    padding: 0.5rem 1rem;
    font-size: 0.9rem;
    letter-spacing: 0.3px;
    transition: all 0.3s ease;
    color: #1c1e21;
  }
  .dm-search-input::placeholder {
    color: #65676b;
  }
  .dm-search-input:focus {
    box-shadow: none;
    background: #e4e6eb !important;
  }
  .dm-search-input:not(:placeholder-shown) {
    color: #111 !important;
    font-weight: 500;
  }
  .dm-search-btn {
    color: #666;
    padding: 0.5rem 1rem;
    transition: color 0.3s ease;
  }
  .dm-search-btn:hover {
    color: #007bff;
  }

  /* Khi input có text hoặc focus, thay đổi màu nền của input-group */
  .dm-search-input:not(:placeholder-shown) ~ .input-group-append,
  .dm-search-input:focus ~ .input-group-append {
    background: #e4e6eb;
  }

  /* User Actions */
  .dm-user-actions {
    display: flex;
    align-items: center;
  }
  .dm-user-actions .dm-nav-link {
    color: #111 !important;
    font-size: 1.1rem;
    padding: 0.4rem !important;
    margin: 0 0.2rem;
  }
  .dm-user-actions .dm-nav-icon {
    font-size: 1.1rem;
    margin: 0;
  }
  .dm-badge {
    font-size: 0.6rem;
    padding: 0.2rem 0.4rem;
    background: #007bff;
    border: 2px solid #fff;
    font-weight: 600;
    letter-spacing: 0.5px;
  }

  /* Notification Menu */
  .dm-notification-menu {
    width: 300px;
    padding: 0;
    border: none;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    border-radius: 10px;
  }
  .dm-notification-header {
    padding: 0.8rem;
    border-bottom: 1px solid #eee;
    display: flex;
    justify-content: space-between;
    align-items: center;
  }
  .dm-notification-header h6 {
    font-weight: 600;
    letter-spacing: 0.5px;
    font-size: 0.9rem;
  }
  .dm-notification-body {
    max-height: 300px;
    overflow-y: auto;
  }
  .dm-notification-item {
        display: flex;
        align-items: flex-start;
    padding: 0.8rem;
    border-bottom: 1px solid #eee;
      }
  .dm-notification-item img {
    width: 35px;
    height: 35px;
    margin-right: 0.8rem;
      }
  .dm-notification-content {
    flex: 1;
  }
  .dm-notification-content p {
    font-size: 0.85rem;
    line-height: 1.4;
    margin-bottom: 0.2rem;
      }
  .dm-notification-content small {
    font-size: 0.75rem;
    letter-spacing: 0.3px;
  }
  .dm-notification-footer {
    padding: 0.4rem;
    border-top: 1px solid #eee;
  }

  /* Dropdown Menu */
  .dm-dropdown-menu {
    border: none;
    box-shadow: 0 5px 15px rgba(0,0,0,0.1);
    border-radius: 10px;
    min-width: 200px;
    padding: 0.5rem 0;
  }
  .dm-dropdown-item {
    padding: 0.6rem 1rem;
    font-size: 0.9rem;
    color: #333;
    letter-spacing: 0.3px;
    display: flex;
    align-items: center;
  }
  .dm-dropdown-icon {
    font-size: 0.9rem;
    margin-right: 0.6rem;
    opacity: 0.7;
    transition: all 0.3s ease;
      }
  .dm-dropdown-item:hover {
    background: #f8f9fa;
    color: #007bff;
  }
  .dm-dropdown-item:hover .dm-dropdown-icon {
    opacity: 1;
    transform: scale(1.1);
      }

  /* Mobile Menu Styles */
  @media (max-width: 991.98px) {
    .navbar-collapse {
      background: white;
      padding: 1rem;
      box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
    }

    .navbar-nav {
      margin-bottom: 1rem;
    }

    .dm-navbar-right {
      flex-direction: column;
      align-items: stretch !important;
    }

    .dm-search-form {
      margin: 0.5rem 0 !important;
      width: 100%;
    }

    .dm-search-form .input-group {
      width: 100%;
    }

    .dm-user-actions {
      justify-content: space-around;
      margin-top: 0.5rem;
      padding: 0.5rem 0;
      border-top: 1px solid #eee;
    }

    .dropdown-menu {
      position: static !important;
      float: none;
      width: 100%;
      margin-top: 0;
      background-color: transparent;
      border: none;
      box-shadow: none;
      padding-left: 1rem;
    }
  }
    </style>

<!-- Scripts -->
<script src="https://kit.fontawesome.com/a076d05399.js"></script>
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />

<!-- Add Bootstrap 4 CDN -->
<link
  rel="stylesheet"
  href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
/>
<script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
<script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>

<script>
  $(document).ready(function() {
    // Initialize tooltips
    $('[data-toggle="tooltip"]').tooltip();

    // Handle dropdowns on mobile
    if ($(window).width() < 992) {
      $('.dropdown-toggle').on('click', function(e) {
        e.preventDefault();
        e.stopPropagation();
        $(this).next('.dropdown-menu').toggleClass('show');
      });

      // Close dropdowns when clicking outside
      $(document).on('click', function(e) {
        if (!$(e.target).closest('.dropdown').length) {
          $('.dropdown-menu').removeClass('show');
        }
      });
    }

    // Handle window resize
    $(window).resize(function() {
      if ($(window).width() >= 992) {
        $('.dropdown-menu').removeClass('show');
      }
    });
  });
</script>
